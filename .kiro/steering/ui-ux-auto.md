# UI UX Pro Max (Kiro)

For UI or UX work in SOLO mode (build, design, create, implement, review, fix, improve interfaces, layouts, components, dashboards, landing pages, forms, styling, accessibility):

1. Read `.kiro/steering/ui-ux-pro-max/SKILL.md` before generating UI code.
2. Run the skill's design-system / search scripts when instructed.
3. Apply design system output and pre-delivery checklist from the skill.

Auto-invoke on UI/UX intent — equivalent to Cursor Agent Skills behavior. Slash command `/ui-ux-pro-max` also works when explicit invocation is needed.
